[package]
name = "jsonschema-macros-standalone"
publish = false
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

# Deliberately only `jsonschema`: generated validators must not need any other crate in scope.
[dependencies]
jsonschema = { path = "../jsonschema", default-features = false, features = ["macros"] }

[lints]
workspace = true
