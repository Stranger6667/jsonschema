[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "jsonschema_testsuite_pyo3"
version = "0.0.0"
requires-python = ">=3.10"

[tool.maturin]
# Correctness is what the suite checks; a release build takes several times longer.
profile = "dev"
